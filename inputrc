# ---
# title:  GNU readline configuration file
# author: emil lenz
# email:  emillenz@protonmail.com
# date:   2024-05-15
# notes:
# - vi-bindings :: most REPL's (irb, gdb,..) use the readline lib, and we want to get vi-bindings everywhere.
# - symlink :: `ln -s ~/.config/inputrc ~/.inputrc`, this is the location needed for readline
# ---

# set editing-mode vi
# set keyseq-timeout 0
